# Database (SQLite - pas d'installation requise)
DB_PATH=data/wms.sqlite

# JWT
JWT_SECRET=change-me-in-production-use-long-random-string
JWT_EXPIRES_IN=15m

# CORS (comma-separated origins)
CORS_ORIGIN=http://localhost:5173

# Port
PORT=3000
